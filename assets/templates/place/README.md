# $name

Game template, generated by WSync

## Getting Started

To build the place in the project root use:

```bash
wsync build
```

To begin syncing open Roblox Studio and start WSync server using:

```bash
wsync serve
```
